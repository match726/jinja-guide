import { Header } from '@/components/ui/header';

import '@/styles/global.css';

const Home: React.FC = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;